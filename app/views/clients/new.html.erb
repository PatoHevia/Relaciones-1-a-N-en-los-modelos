<h1>Nuevo Cliente</h1>

<%= form_with model: @client, local: true do |form| %>
  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :email %>
    <%= form.email_field :email, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :movie_id, "Película" %>
    <%= form.collection_select :movie_id, Movie.all, :id, :title, prompt: "Selecciona una película", class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.submit "Crear Cliente", class: "btn btn-primary" %>
  </div>
<% end %>
